import 'package:flutter/material.dart';

class LuckyFlutterMarker extends StatelessWidget {
  const LuckyFlutterMarker({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('marcador rojo'),
    );
  }
}