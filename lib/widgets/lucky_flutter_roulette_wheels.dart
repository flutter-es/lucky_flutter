import 'package:flutter/material.dart';

class LuckyFlutterRouletteWheels extends StatelessWidget {
  const LuckyFlutterRouletteWheels({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('las ruedas@'),
    );
  }
}
